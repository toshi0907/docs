{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "jekyll: build",
            "type": "shell",
            "command": "cd docs && export PATH=\"$PATH:/home/runner/.local/share/gem/ruby/3.2.0/bin\" && bundle exec jekyll build && bundle exec jekyll serve --host 0.0.0.0 --port 4000",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}